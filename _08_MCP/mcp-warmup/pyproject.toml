[project]
name = "mcp-warmup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sathish15g", email = "gsathishkumar15@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.7",
]

[project.scripts]
mcp-warmup = "mcp_warmup:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
